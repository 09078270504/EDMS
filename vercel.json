{
  "version": 2,
  "buildCommand": "if [ -n \"$DATABASE_URL\" ]; then python webocr/manage.py migrate --noinput; python webocr/manage.py create_default_superuser; else echo 'Skipping migrate & superuser: DATABASE_URL not set'; fi && python webocr/manage.py collectstatic --noinput",
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "50mb",
        "includeFiles": [
          "webocr/staticfiles/**",
          "webocr/static/**",
          "webocr/templates/**"
        ]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.py" }
  ]
}
