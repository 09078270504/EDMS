{
  "functions": {
    "api/index.py": {
      "runtime": "python3.11",
      "maxDuration": 300
    }
  },
  "buildCommand": "pip install -r requirements.txt && cd webocr && python manage.py collectstatic --noinput && cd .. && mkdir static && xcopy webocr\\staticfiles static /E /I /Y",
  "routes": [
    { "src": "/static/(.*)", "dest": "/static/$1" },
    { "src": "/(.*)", "dest": "/api/index.py" }
  ]
}
